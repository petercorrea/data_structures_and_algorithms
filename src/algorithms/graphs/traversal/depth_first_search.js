// Depth first search begins by recursively traversing the children of each node, i.e.
// start at the parent, visit one child, visit the children of that node, and so on,
// until a leaf node is reached.
